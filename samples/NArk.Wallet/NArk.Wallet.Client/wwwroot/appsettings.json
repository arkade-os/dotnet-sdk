{
  "//": "Blazor WebAssembly loads this file at startup. It is served to the browser as-is, so it holds addresses only — never a secret. No solver is named here or anywhere in this build: which ones exist is answered by the public solver registry at runtime.",

  "ArkadeLightning": {
    "//": "Optional. Both corridors work without it; what a covclaimd adds is a daemon that races our own claim, so a funded receive is still collected while this wallet is closed.",
    "CovclaimdUrl": ""
  }
}
